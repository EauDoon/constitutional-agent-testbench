# README Quick Start Audit - 2026-09-23

Repo: constitutional-agent-testbench
Default branch: main

Finding: Quick start already present within first 60 lines.

Existing surface:
- Lines 43 to 75: explicit "## Quick start" heading with PYTHONPATH setup,
  pip install, and two example invocations.

Concrete steps visible:
1. export PYTHONPATH=src (or $env:PYTHONPATH = "src" in PowerShell)
2. python -m pip install --no-deps .
3. python -m constitutional_agent_testbench.cli validate-policy examples/policy.json
4. python -m constitutional_agent_testbench.cli evaluate examples/policy.json examples/passing-response.json

Action: surface-confirmed. Skip.